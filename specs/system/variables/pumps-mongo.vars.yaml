component: pumps-mongo
variables:
    - name: capped_collection_size_is_int64
      type: bool
      direction: input
      description: MongoDB driver returns capped-collection size values as int64 (currently violated under mongo:6; retained as an external assumption, not a product KnownIssue).
