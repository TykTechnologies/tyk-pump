# Generated by: tyk-ci/wf-gen
# Generated on: Tuesday 16 March 2021 10:01:17 AM UTC

# Generation commands:
# ./pr.zsh -branch goreleaser/fixes -title rel-eng: -rc tags and unstable repos
# m4 -E -DxREPO=tyk-pump

FROM debian:buster-slim
RUN apt-get update \
 && apt-get dist-upgrade -y ca-certificates \
 && apt-get autoremove -y

COPY *.deb /
RUN dpkg -i /*.deb && rm /*.deb

ARG PORTS

EXPOSE $PORTS

ENTRYPOINT ["/opt/tyk-pump/tyk-pump" ]
CMD [ "--conf=/opt/tyk-pump/tyk-pump.conf" ]
