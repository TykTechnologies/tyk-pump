# Generated by: tyk-ci/wf-gen
# Generated on: Wed 17 Feb 18:40:41 UTC 2021

# Generation commands:
# ./pr.zsh -title more config files for slim -branch goreleaser/more -p -repos tyk-pump,tyk-sink
# m4 -E -DxREPO=tyk-pump


# Check the documentation at http://goreleaser.com
# This project needs CGO_ENABLED=1 and the cross-compiler toolchains for
# - arm64
# - macOS (only 10.15 is supported)
# - amd64

builds:
  - id: std-linux
    ldflags:
      - -X main.VERSION={{.Version}} -X main.commit={{.Commit}} -X main.buildDate={{.Date}} -X main.builtBy=goreleaser
    goos:
      - linux
    goarch:
      - amd64
  - id: std-darwin
    ldflags:
      - -X main.VERSION={{.Version}} -X main.commit={{.Commit}} -X main.buildDate={{.Date}} -X main.builtBy=goreleaser
    env:
      - CC=o64-clang
    goos:
      - darwin
    goarch:
      - amd64
  - id: std-arm64
    ldflags:
      - -X main.VERSION={{.Version}} -X main.commit={{.Commit}} -X main.buildDate={{.Date}} -X main.builtBy=goreleaser
    env:
      - CC=aarch64-linux-gnu-gcc
    goos:
      - linux
    goarch:
      - arm64
  # static builds strip symbols and do not allow plugins
  - id: static-amd64
    ldflags:
      - -s -w -X main.VERSION={{.Version}} -X main.commit={{.Commit}} -X main.buildDate={{.Date}} -X main.builtBy=goreleaser
      - -linkmode external -extldflags -static
    goos:
      - linux
    goarch:
      - amd64


dockers:
  - ids:
      - std
    image_templates:
      - "tykio/tyk-pump:{{ .Tag }}"
      - "tykio/tyk-pump:v{{ .Major }}.{{ .Minor }}"
      - "{{ .Env.ECR_REGISTRY }}:{{ .Env.IMAGE_TAG }}"
      - "{{ .Env.ECR_REGISTRY }}:{{ .Commit }}"
      - "{{ .Env.ECR_REGISTRY }}:latest"
    build_flag_templates:
      - "--build-arg=PORTS=80"
      - "--label=org.opencontainers.image.created={{.Date}}"
      - "--label=org.opencontainers.image.title={{.ProjectName}}"
      - "--label=org.opencontainers.image.revision={{.FullCommit}}"
      - "--label=org.opencontainers.image.version={{.Version}}"
    goarch: amd64
    goos: linux
    dockerfile: Dockerfile.std
    extra_files:
      - "LICENSE.md"
      - "pump.example.conf"
  - ids:
      - static-amd64
    image_templates:
      - "tykio/tyk-pump:slim"
      - "tykio/tyk-pump:s{{ .Major }}.{{ .Minor }}"
    build_flag_templates:
      - "--build-arg=PORTS=80"
      - "--label=org.opencontainers.image.created={{.Date}}"
      - "--label=org.opencontainers.image.title={{.ProjectName}}-slim"
      - "--label=org.opencontainers.image.revision={{.FullCommit}}"
      - "--label=org.opencontainers.image.version={{.Version}}"
    goarch: amd64
    goos: linux
    dockerfile: Dockerfile.slim
    extra_files:
      - "LICENSE.md"
      - "pump.example.conf"

nfpms:
  - id: std
    vendor: "Tyk Technologies Ltd"
    homepage: "https://tyk.io"
    maintainer: "Tyk <info@tyk.io>"
    description: Archive analytics for the Tyk API gateway
    package_name: tyk-pump
    version_metadata: git
    builds:
      - std-linux
      - std-arm64
    formats:
      - deb
      - rpm
    contents:
      - src: "README.md"
        dst: "/opt/share/docs/tyk-pump/README.md"
      - src: "LICENSE.md"
        dst: "/opt/share/docs/tyk-pump/LICENSE.md"
      - src: "install/*"
        dst: "/opt/tyk-pump/install"
      - src: pump.example.conf
        dst: /opt/tyk-pump/pump.conf
        type: "config|noreplace"
    scripts:
      preinstall: "install/before_install.sh"
      postinstall: "install/post_install.sh"
      postremove: "install/post_remove.sh"
    bindir: "/opt/tyk-pump"
    overrides:
      rpm:
        replacements:
          amd64: x86_64
          arm: aarch64
      deb:
        replacements:
          arm: arm64
    rpm:
      signature:
        key_file: tyk.io.signing.key
    deb:
      signature:
        key_file: tyk.io.signing.key
        type: origin


archives:
- id: std-linux
  builds:
    - std-linux
  files:
    - README.md
    - "LICENSE.md"
    - CHANGELOG.md
    - "install/*"
    - pump.example.conf

- id: std-arm64
  builds:
    - std-arm64
  files:
    - README.md
    - "LICENSE.md"
    - CHANGELOG.md
    - "install/*"
    - pump.example.conf
- id: static-amd64
  name_template: "{{ .ProjectName }}_{{ .Version }}_static_{{ .Os }}_{{ .Arch }}"
  builds:
    - static-amd64
  files:
    - README.md
    - "LICENSE.md"
    - CHANGELOG.md
    - "install/*"
    - pump.example.conf


checksum:
  disable: false

signs:
  - id: std
    artifacts: checksum

changelog:
  sort: asc
  filters:
    exclude:
    - '^utils:'
    - (?i)typo
    - 'Merge (pull request|branch)'
