# Generated by: tyk-ci/wf-gen
# Generated on: Thu 22 Apr 19:28:57 UTC 2021

# Generation commands:
# ./meta.zsh -repos tyk-pump
# m4 -E -DxSRC_BRANCH='master' -DxRELEASE_BRANCHES='release-0.8, release-1.0, release-1.2.0  release-1.2.1 release-1.3' -DxAUTO_FILES='.goreleaser.yml Dockerfile.std Dockerfile.slim aws/byol.pkr.hcl .github/workflows/release.yml .github/workflows/del-env.yml integration/terraform/outputs.tf'

# This workflow will silently ignore FILES that are missing in master
# Most of the time this is what we want, older repos may not have some files

name: Sync automation

on:
  push:
    branches:
      - master
    paths:
      - .github/workflows/*
      - integration/*
      - .goreleaser.yml

env:
  FILES: .goreleaser.yml Dockerfile.std Dockerfile.slim aws/byol.pkr.hcl .github/workflows/release.yml .github/workflows/del-env.yml integration/terraform/outputs.tf
  
jobs:
  sync:
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        branch: [ release-0.8, release-1.0, release-1.2.0  release-1.2.1 release-1.3 ]
          
    steps:
      - uses: actions/checkout@v2
        with:
          ref: ${{matrix.branch}}

      - name: sync ${{matrix.branch}} from master
        run: |
          git fetch origin master
          git config --local user.email "wf-gen@tyk-ci"
          git config --local user.name "Bender"
          for f in $FILES; do 
            git checkout --theirs origin/master -- $f || true
          done
          git commit -a -m "[CI] Syncing release automation from master"
          git push origin
          echo "::debug::${FILES} syncd for ${{matrix.branch}}"
