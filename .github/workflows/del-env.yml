

# Generated by: tyk-ci/wf-gen
# Generated on: Wednesday 02 March 2022 07:10:51 AM UTC

# Generation commands:
# ./pr.zsh -title Sync from latest releng templates -branch TD-883/consolidate-releng-code -base TD-883/consolidate-releng-code -repos tyk-pump -p
# m4 -E -DxREPO=tyk-pump

name: Retiring dev env

on:
  delete:
    branches:
      - feature/*
      - integration/*

jobs:
  retire:
    runs-on: ubuntu-latest

    steps:
      - name: Tell gromit about deleted branch
        run: |
            curl -fsSL -H "Authorization: ${{secrets.GROMIT_TOKEN}}" "https://domu-kun.cloud.tyk.io/gromit/env/${GITHUB_REF##*/}" \
                 -X DELETE
