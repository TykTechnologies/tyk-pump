# Generated by: tyk-ci/wf-gen
# Generated on: Wed 24 Feb 15:14:03 UTC 2021

# Generation commands:
# ./pr.zsh -title fixes from elsewhere -branch goreleaser/more -repos tyk-pump,tyk-sink -p
# m4 -E -DxREPO=tyk-pump

name: Retiring dev env

on:
  delete:
    branches:
      - feature/*
      - integration/*

jobs:
  retire:
    runs-on: ubuntu-latest

    steps:
      - name: Tell gromit about deleted branch
        run: |
            curl -fsSL -H "Authorization: ${{secrets.GROMIT_TOKEN}}" "https://domu-kun.cloud.tyk.io/gromit/env/${GITHUB_REF##*/}" \
                 -X DELETE
