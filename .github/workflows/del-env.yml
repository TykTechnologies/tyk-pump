# Generated by: tyk-ci/wf-gen
# Generated on: Wed 16 Jun 11:46:09 UTC 2021

# Generation commands:
# ./pr.zsh -title releng: [TD-388] rpm -U for upgrades -branch releng/master -repos tyk-pump
# m4 -E -DxREPO=tyk-pump

name: Retiring dev env

on:
  delete:
    branches:
      - feature/*
      - integration/*

jobs:
  retire:
    runs-on: ubuntu-latest

    steps:
      - name: Tell gromit about deleted branch
        run: |
            curl -fsSL -H "Authorization: ${{secrets.GROMIT_TOKEN}}" "https://domu-kun.cloud.tyk.io/gromit/env/${GITHUB_REF##*/}" \
                 -X DELETE
