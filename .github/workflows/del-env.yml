# Generated by: tyk-ci/wf-gen
# Generated on: Tue 16 Mar 02:38:53 UTC 2021

# Generation commands:
# ./pr.zsh -title Fix github release title -branch goreleaser/fixes
# m4 -E -DxREPO=tyk-pump

name: Retiring dev env

on:
  delete:
    branches:
      - feature/*
      - integration/*

jobs:
  retire:
    runs-on: ubuntu-latest

    steps:
      - name: Tell gromit about deleted branch
        run: |
            curl -fsSL -H "Authorization: ${{secrets.GROMIT_TOKEN}}" "https://domu-kun.cloud.tyk.io/gromit/env/${GITHUB_REF##*/}" \
                 -X DELETE
