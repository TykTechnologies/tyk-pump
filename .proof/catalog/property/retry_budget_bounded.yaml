# Draft class — generated by `proof catalog draft --from-signal go.exponential_backoff_no_deadline`
# Source rule: go.exponential-backoff-no-deadline (pack signal-rules/tyk-pump-phase-n.yaml)
# Framework hints: CWE-1088, CWE-1322, NIST-SP-800-53-SC-5
#
# Review summary/applies_when/frameworks before landing.
# Stubs marked with `# TODO:` need agent or human elaboration.

id: retry_budget_bounded
name: retry_budget_bounded
category: property
status: active
summary: Retry loops shall be bounded by an absolute wall-clock or context deadline, not just an attempt count; the worst-case elapsed time at maximum retries shall be explicit and shall not exceed the surrounding cycle interval.
description: Drafted from signal go.exponential_backoff_no_deadline at signal-rules/tyk-pump-phase-n.yaml. Elaborate per the failure modes named by framework_hints CWE-1088, CWE-1322, NIST-SP-800-53-SC-5.
applies_when:
  description: code uses bounded-retry-with-backoff (cenkalti/backoff, aws/retry, custom while-loop) where the bound is a count rather than a deadline
frameworks:
  - id: CWE
    refs:
      - CWE-1088
      - CWE-1322
  - id: NIST-800-53
    refs:
      - NIST-SP-800-53-SC-5
recommended_evidence:
  - kind: boundary
    note: "# TODO: elaborate the evidence shape"
  - kind: review
    note: "# TODO: elaborate the evidence shape"
  - kind: scenario
    note: "# TODO: elaborate the evidence shape"
