# Draft class — generated by `proof catalog draft --from-signal go.multi_pump_fanout`
# Source rule: go.multi-pump-fanout (pack signal-rules/tyk-pump-phase-n.yaml)
# Framework hints: CWE-1088, CWE-770, NIST-SP-800-53-SC-5
#
# Review summary/applies_when/frameworks before landing.
# Stubs marked with `# TODO:` need agent or human elaboration.

id: cascade_circuit_breaker
name: cascade_circuit_breaker
category: domain
status: active
summary: When one consumer in a peer-fanout repeatedly fails or saturates, downstream / sibling consumers shall be circuit-broken — the failure shall not propagate indefinitely.
description: Drafted from signal go.multi_pump_fanout at signal-rules/tyk-pump-phase-n.yaml. Elaborate per the failure modes named by framework_hints CWE-1088, CWE-770, NIST-SP-800-53-SC-5.
applies_when:
  description: code fans out to multiple peer consumers and any single consumer failure could affect throughput, latency, or correctness of the others
frameworks:
  - id: CWE
    refs:
      - CWE-1088
      - CWE-770
  - id: NIST-800-53
    refs:
      - NIST-SP-800-53-SC-5
recommended_evidence:
  - kind: boundary
    note: "# TODO: elaborate the evidence shape"
  - kind: review
    note: "# TODO: elaborate the evidence shape"
  - kind: scenario
    note: "# TODO: elaborate the evidence shape"
