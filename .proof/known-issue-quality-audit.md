# KnownIssue / Risk Quality Audit

Date: 2026-06-26
Repo: TykTechnologies/tyk-pump
Branch reviewed locally: feat/reqproof-coverage

## Scope

This audit reviews active KnownIssue, accepted risk, and KI-backed tracked-debt
records for evidence quality, provenance, severity, and closure readiness. It
does not fix product behavior. Real unfixed behavior remains a KnownIssue or
accepted risk; historically fixed behavior belongs in DEFECT records.

Current inventory at audit start:

- Active KnownIssues: 143
- Withdrawn KnownIssues: 1
- Active accepted risks: 13
- Active waivers: 0
- KI-backed code-signal debt rows: 167
- KI-backed MC/DC debt rows: 1
- Raw requirement `ki_ref` deferral rows: 416 across 78 requirement files,
  covering 111 unique KnownIssue IDs
- Go `mcdc:ignore` rows: 291 across 66 Go files; 216 rows carry an explicit
  KnownIssue marker or name, covering 20 unique KnownIssue IDs
- Deferred acceptance-criteria debt rows: 1

Subagent inventory cross-check: all 13 accepted risks, all 111 unique
requirement `ki_ref` KnownIssue IDs, and all 20 unique MC/DC KnownIssue IDs are
represented in the per-item audit rows below.

## Verdicts

- `valid`: record is well formed; evidence/reproducer matches the stated issue;
  provenance, affected requirements, severity, disposition, and review metadata
  are adequate.
- `needs_metadata_hardening`: behavior/evidence is sound, but fields such as
  review date, origin, owner, affected requirements, severity rationale, or
  release disposition need tightening.
- `needs_reproducer_hardening`: issue is plausible, but the reproducer is too
  weak, too broad, missing, or does not exercise the stated defect.
- `needs_split`: one record covers multiple defect classes that should be
  tracked independently.
- `needs_merge`: multiple records duplicate the same defect class.
- `stale_or_resolved`: current code or proof state indicates the record may no
  longer be active and should be closed or converted.
- `invalid`: record does not describe a real product/proof debt item.

## Review Log

| Item | Type | Verdict | Evidence Checked | Action |
| --- | --- | --- | --- | --- |
| `elapsed-timesince-dst-obligation-misattributed` | KnownIssue | `needs_metadata_hardening` -> reviewed | Static reproducer `rg -n "time\\.Since\\(" main.go pumps/kafka.go pumps/sqs.go retry/http-retry.go`; source sites are elapsed-duration metrics/logging, not date-shard business arithmetic. | Refreshed `.proof/evidence/ki-elapsed-timesince-dst-obligation-misattributed.yaml`; keep open as proof/spec attribution debt, not product defect. |
| `elasticsearch-api-key-auth-dropped-when-use-ssl` | KnownIssue | `valid` | Ran `go test -p=1 -count=1 -timeout=20m ./pumps -run '^TestElasticsearchPump_ApiKeyAuthDroppedWhenUseSSL_KI$'`; inspected `pumps/elasticsearch.go:getOperator`, where API-key transport is assigned before the `UseSSL` branch replaces `httpClient` with a TLS-only transport. | Stamped review date `2026-11-30`; refreshed `.proof/evidence/ki-elasticsearch-api-key-auth-dropped-when-use-ssl.yaml`; no product fix made. |
| `elasticsearch-decode-base64-errors-silent-empty` | KnownIssue | `valid` | Ran `go test -p=1 -count=1 -timeout=20m ./pumps -run '^TestGetMapping_DecodeBase64MalformedInput_KI$'`; inspected `getMapping` malformed base64 behavior. | Refreshed `.proof/evidence/ki-elasticsearch-decode-base64-errors-silent-empty.yaml`; already reviewed through `2026-11-30`. |
| `elasticsearch-mcp-routing-non-bulk-ignored` | KnownIssue | `valid` | Ran `go test -p=1 -count=1 -timeout=20m ./pumps -run '^TestElasticsearchPump_WriteData_MCPIndexRouting_NonBulkBug$'`; inspected non-bulk `processData` builder using the default index instead of per-record `recordIndex`. | Stamped review date `2026-11-30`; refreshed `.proof/evidence/ki-elasticsearch-mcp-routing-non-bulk-ignored.yaml`; no product fix made. |
| `elasticsearch-unbounded-reconnect-recursion` | KnownIssue | `valid` | Inspected `pumps/elasticsearch.go:connect` recursive retry and `WriteData` nil-operator reconnect recursion; evidence is the OpenGrep signal `go.recursion.unbounded-on-error`. | Attached and refreshed `.proof/evidence/ki-elasticsearch-unbounded-reconnect-recursion.yaml`; no product fix made. |
| `elasticsearch-writedata-errors-swallowed` | KnownIssue | `valid` | Ran `go test -p=1 -count=1 -timeout=20m ./pumps -run '^TestElasticsearchPump_WriteData_V7ProcessDataIndexError$'`; inspected `WriteData` ignoring `operator.processData` errors. | Stamped review date `2026-11-30`; refreshed `.proof/evidence/ki-elasticsearch-writedata-errors-swallowed.yaml`; no product fix made. |
| `env-prefix-const-typo` | KnownIssue | `needs_metadata_hardening` -> reviewed | Static source check: `config.go` declares and uses `ENV_PREVIX`; string value remains correct (`TYK_PMP`), so this is naming/docs debt rather than runtime failure. | Stamped review date `2026-11-30`; keep disposition `ship`. |
| `es-legacy-versions-need-deprecated-containers` | KnownIssue | `valid` | Inspected `pumps/elasticsearch.go` v3/v5/v6 branches and `pumps/elasticsearch_mcdc_100_test.go` legacy operator branch tests; issue is environment/capability debt for true legacy ES wire coverage. | Already had review date `2026-11-30`; no change needed. |

## Accepted Risk Slice

Read-only subagent audit completed for all 13 accepted risks.
Follow-up sidecar audit found that the current AcceptedRisk schema does not
define structured `kind`, `linked_known_issues`, or `evidence_manifests`
fields. Those links remain prose/backing-KI mediated until proof supports them
as first-class risk fields.

| Item | Type | Verdict | Evidence Checked | Action |
| --- | --- | --- | --- | --- |
| `elasticsearch-unbounded-reconnect-stack-overflow` | AcceptedRisk | `valid` | Matches KI `elasticsearch-unbounded-reconnect-recursion`, reviewed through `2026-11-30`. | No change needed. |
| `graylog-moesif-record-fatal` | AcceptedRisk | `needs_metadata_hardening` -> broadened | Risk originally accepted per-record fatal paths, while backing KI `graylog-moesif-logfatal-on-record-error` also covers Moesif parse/config fatal behavior. | Broadened title, description, mitigation, customer impact, and affected requirements to include `SW-REQ-049` and `SW-REQ-052`. |
| `mongo-pump-context-timeout-bypass` | AcceptedRisk | `needs_metadata_hardening` -> partially hardened | Backing KI `mongo-pump-ignores-caller-context` affects both `SYS-REQ-005` and `INT-REQ-004`; risk only listed `SYS-REQ-005`. | Added `INT-REQ-004` to accepted risk. |
| `no-dlq-on-pump-write-failure` | AcceptedRisk | `valid` | Matches KI `write-failure-after-pop-loses-records` and catalog class `dead_letter_or_reenqueue_on_total_write_failure`. | No change needed. |
| `per-pump-timeout-not-enforced-when-zero` | AcceptedRisk | `needs_reproducer_hardening` -> hardened | Backing KI `pump-no-timeout-can-block-purge-cycle` pointed at an Elasticsearch HTTP timeout signal rather than the `main.go` zero-timeout branch. | Added and refreshed `.proof/evidence/ki-pump-no-timeout-can-block-purge-cycle.yaml`, linked it to the backing KI, and refreshed current `main.go` line anchors. |
| `pump-config-typos-silently-ignored` | AcceptedRisk | `valid` | Matches KI `mapstructure-decode-silently-drops-unknown-keys` and catalog class `config_schema_strict`. | No change needed. |
| `pump-panic-crashes-process` | AcceptedRisk | `needs_merge` -> merged | KIs `no-panic-recovery-in-exec-pump-writing` and `pump-fanout-panic-not-recovered` describe the same accepted panic surface. | Canonicalized on `pump-fanout-panic-not-recovered`, linked this accepted risk to that KI, and retained `no-panic-recovery-in-exec-pump-writing` as a reviewed legacy alias rather than a separate product issue. |
| `pump-writedata-swallows-per-batch-errors` | AcceptedRisk | `valid` | Matches KI `pump-writedata-swallows-per-batch-errors`, reviewed through `2026-11-30`. | No change needed. |
| `serializer-protobuf-city-names-data-loss` | AcceptedRisk | `valid` | Matches KI `serializer-protobuf-loses-city-names`. | No change needed. |
| `sql-default-migration-today-only-data-loss` | AcceptedRisk | `valid` | Matches KI `sql-default-migration-today-only`. | No change needed. |
| `statsd-setup-fatal-aborts-pump` | AcceptedRisk | `valid` | Matches KI `logfatal-on-statsd-setup` and catalog class `process_exit_on_recoverable`. | No change needed. |
| `storage-pop-expire-non-atomic-data-loss` | AcceptedRisk | `needs_metadata_hardening` -> partially hardened | Backing KI `getanddeleteset-expire-fail-loses-records` includes `STK-REQ-002`; accepted risk only listed `INT-REQ-005`. | Added `STK-REQ-002` to accepted risk. |
| `uptime-pump-silent-failure` | AcceptedRisk | `valid` | Matches KI `uptime-write-has-no-error-return`. | No change needed. |

## KnownIssue Slice: Aggregate Through SQLite Docs

Read-only subagent audit completed for 18 KnownIssues from
`aggregate-counter-replay-not-idempotent` through
`docs-sqlite-still-listed-as-supported-sql-type`.

| Item | Type | Verdict | Evidence Checked | Action |
| --- | --- | --- | --- | --- |
| `aggregate-counter-replay-not-idempotent` | KnownIssue | `needs_reproducer_hardening` -> hardened | Focused analytics tripwire now compares one record with the same record replayed twice and asserts the current overcount. | Refreshed `.proof/evidence/ki-aggregate-counter-replay-not-idempotent.yaml`; Mongo `$inc` path remains described in the KI, but executable reproducer now covers the core additive aggregate behavior. |
| `aggregate-mcdc-unreachable-string-assertion-ok` | KnownIssue | `valid` | Current `analytics/aggregate.go` contains the expected `//mcdc:ignore` comments and structural scan evidence for the string-field type assertions. | Attached and refreshed `.proof/evidence/ki-aggregate-mcdc-unreachable-string-assertion-ok.yaml`; keep as structural MC/DC proof debt unless/until the defensive assertions are removed. |
| `analytics-key-shard-ceiling-hardcoded` | KnownIssue | `needs_metadata_hardening` -> hardened | Active code fact at `main.go:267` is now explicitly bounded by `SYS-REQ-030`'s verified gateway producer contract for the legacy key plus shards `_0` through `_9`. | Clarified environment note and set `release_disposition: ship_with_known_issue`; no product code changed. |
| `analytics-timestamp-timezone-convention-unpinned` | KnownIssue | `needs_split` -> split | Record combined timestamp convention, shard routing, aggregate/uptime serialization, and DST formatting paths. | Narrowed this KI to analytics-family `tz_explicit` timestamp convention debt; moved SQL-family `temporal_window_inclusive` shard-routing debt to `sql-family-day-shard-timezone-convention-unpinned` with a six-surface static tripwire; DST attribution remains under `elapsed-timesince-dst-obligation-misattributed`. |
| `analytics-trimstring-byte-truncation-not-unicode-safe` | KnownIssue | `needs_reproducer_hardening` -> hardened | Focused unit tripwire cuts a multibyte UTF-8 rune and asserts the current invalid string output. | Refreshed `.proof/evidence/ki-analytics-trimstring-byte-truncation-not-unicode-safe.yaml`; product behavior remains unchanged. |
| `common-setloglevel-nil-log-panic` | KnownIssue | `needs_reproducer_hardening` -> hardened | Focused unit tripwire calls `SetLogLevel` on a zero-value `CommonPumpConfig` and asserts the current panic. | Refreshed `.proof/evidence/ki-common-setloglevel-nil-log-panic.yaml`; loader-order mitigation remains unchanged. |
| `config-pump-type-whitelist-not-enforced-at-load` | KnownIssue | `needs_reproducer_hardening` -> hardened | Focused loader-boundary tripwire proves unknown preloaded/env pump types survive `LoadConfig` even though `GetPumpByName` rejects the type later. | Refreshed `.proof/evidence/ki-config-pump-type-whitelist-not-enforced-at-load.yaml`; no startup behavior changed. |
| `csv-writedata-nil-file-handle-panic` | KnownIssue | `needs_metadata_hardening` -> hardened | Existing focused tests cover create/open failure behavior and now have a live proof evidence manifest. | Attached and refreshed `.proof/evidence/ki-csv-writedata-nil-file-handle-panic.yaml`; backlog remains only if we want a stricter panic-specific witness. |
| `docs-elasticsearch-mcp-index-name-undocumented` | KnownIssue | `needs_reproducer_hardening` -> hardened | Source-positive/docs-negative reproducer now pairs `MCPIndexName`/routing code with tyk-docs absence for `mcp_index_name` and MCP index wording. | Attached and refreshed `.proof/evidence/ki-docs-elasticsearch-mcp-index-name-undocumented.yaml`; tyk-docs fix remains backlog. |
| `docs-elasticsearch-version-range-incorrect` | KnownIssue | `valid` | Source/version scan still shows supported ES 3/5/6/7 branches while tyk-docs still says `Elasticsearch (2.0 - 7.x)`. | Attached and refreshed `.proof/evidence/ki-docs-elasticsearch-version-range-incorrect.yaml`; docs remediation remains backlog. |
| `docs-health-endpoint-liveness-vs-readiness-unclear` | KnownIssue | `needs_reproducer_hardening` -> hardened | Reproducer now pairs source/requirement evidence that `/health` is a liveness-style status endpoint with docs that mention health but not readiness/liveness semantics. | Attached and refreshed `.proof/evidence/ki-docs-health-endpoint-liveness-vs-readiness-unclear.yaml`; docs clarification remains backlog. |
| `docs-hybrid-enable-mcp-aggregation-undocumented` | KnownIssue | `needs_reproducer_hardening` -> hardened | Source-positive/docs-negative reproducer now pairs `enable_mcp_aggregation` code and requirement evidence with tyk-docs absence. | Attached and refreshed `.proof/evidence/ki-docs-hybrid-enable-mcp-aggregation-undocumented.yaml`; docs remediation remains backlog. |
| `docs-kinesis-env-streamname-typo` | KnownIssue | `valid` | README typo and code-side correct `StreamName`/`stream_name` terms are captured in a refreshed reproducer manifest. | Attached and refreshed `.proof/evidence/ki-docs-kinesis-env-streamname-typo.yaml`; README/docs correction remains backlog. |
| `docs-mcp-pump-family-entirely-undocumented` | KnownIssue | `needs_metadata_hardening` -> hardened | Description covers `SW-REQ-038`, `SW-REQ-039`, `SW-REQ-044`, and `SW-REQ-045`; reproducer now proves MCP pump registration/source docs exist while tyk-docs lacks MCP pump-family coverage. | Added missing affected requirements earlier and attached/refreshed `.proof/evidence/ki-docs-mcp-pump-family-entirely-undocumented.yaml`; keep separate from the per-pump config-section KI. |
| `docs-missing-per-pump-config-sections` | KnownIssue | `needs_reproducer_hardening` -> hardened | Reproducer now compares registered pump families to tyk-docs `pump-config.md` headings for missing per-pump config sections. | Attached and refreshed `.proof/evidence/ki-docs-missing-per-pump-config-sections.yaml`; keep separate from the MCP-family docs KI because this is broader per-pump configuration coverage. |
| `docs-splunk-mutates-default-transport-undocumented` | KnownIssue | `needs_reproducer_hardening` -> hardened | Source-positive/docs-negative reproducer now pairs Splunk global/default HTTP transport mutation evidence with tyk-docs absence for that operational constraint. | Attached and refreshed `.proof/evidence/ki-docs-splunk-mutates-default-transport-undocumented.yaml`; docs callout remains backlog. |
| `docs-sql-migrate-sharded-tables-undocumented` | KnownIssue | `needs_reproducer_hardening` -> hardened | Source-positive/docs-negative reproducer now pairs `MigrateShardedTables`/`migrate_sharded_tables` code and requirement evidence with tyk-docs absence. | Attached and refreshed `.proof/evidence/ki-docs-sql-migrate-sharded-tables-undocumented.yaml`; docs remediation remains backlog. |
| `docs-sqlite-still-listed-as-supported-sql-type` | KnownIssue | `needs_metadata_hardening` -> hardened | Docs still list sqlite while `pumps/sql.go` rejects it; `git log -S sqlite -- pumps/sql.go` identifies removal commit. | Changed `introduced_in` from `inception` to `775f8e3` earlier and attached/refreshed `.proof/evidence/ki-docs-sqlite-still-listed-as-supported-sql-type.yaml`. |

## KnownIssue Slice: External Endpoints Through Kinesis

Read-only subagent audit completed for 29 KnownIssues from
`external-pump-endpoints-no-ssrf-allowlist` through
`kinesis-splitintobatches-zero-infinite-loop`.

| Item | Type | Verdict | Evidence Checked | Action |
| --- | --- | --- | --- | --- |
| `external-pump-endpoints-no-ssrf-allowlist` | KnownIssue | `needs_metadata_hardening` -> reviewed | Static reproducer cites outbound endpoint fields across Hybrid, Influx, Splunk, Logz.io, and Elasticsearch. | Refreshed `.proof/evidence/ki-external-pump-endpoints-no-ssrf-allowlist.yaml`; backlog: cite exact endpoint fields per pump in description. |
| `filterdata-base64-decode-silent-noop` | KnownIssue | `needs_reproducer_hardening` -> partially hardened | Focused test `TestFilterDataBase64DecodeFailurePreservesField` exists and was run by subagent. | Added canonical `// Reproduces: filterdata-base64-decode-silent-noop`, test evidence pointer, and refreshed `.proof/evidence/ki-filterdata-base64-decode-silent-noop.yaml`. |
| `getanddeleteset-expire-fail-loses-records` | KnownIssue | `needs_reproducer_hardening` -> partially hardened | Existing test `TestTemporalStorageHandler_GetAndDeleteSet_ExpireFailureDecision` exercises successful pop followed by `Expire` failure. | Added canonical `// Reproduces: getanddeleteset-expire-fail-loses-records`; stamped review date `2026-11-30`; refreshed `.proof/evidence/ki-getanddeleteset-expire-fail-loses-records.yaml`. |
| `getanddeleteset-expire-ttl-assumes-clock-sync` | KnownIssue | `stale_or_resolved` -> withdrawn | Current code uses relative Redis `EXPIRE`; `storage/clock_skew_tolerated_test.go` proves clock-skew tolerance for `SW-REQ-006`. The prior `SW-REQ-007` deferral was on the wrong requirement. | Marked KI `status: withdrawn`, `release_disposition: not_in_scope`; removed the stale `SW-REQ-007` `clock_skew_tolerated` checklist/deferral while leaving the SW-REQ-006 evidence intact. |
| `graph-mcp-sql-sharded-indexes-untracked` | KnownIssue | `valid` | Source still shows Graph/MCP sharded paths lack explicit per-shard index contract/tests. | Refreshed `.proof/evidence/ki-graph-mcp-sql-sharded-indexes-untracked.yaml`. |
| `graph-sql-aggregate-atomicity-fault-injection-missing` | KnownIssue | `needs_reproducer_hardening` -> partially hardened | SQLite callback witness now injects a Graph SQL aggregate `Create` failure, proves the error reaches the caller, and proves no rows commit; a static tripwire pins the remaining missing explicit transaction/isolation declaration. | Added `TestGraphSQLAggregateDoAggregatedWritingReturnsCreateErrorWithoutRows_KI` and `TestGraphSQLAggregateTransactionIsolationHarnessMissing_KI`, removed the `atomicity` and `errors_propagated` deferrals from `SW-REQ-043`, and narrowed remaining debt to `transaction_isolation_declared`. |
| `graph-sql-aggregate-migrate-sharded-tables-ignored` | KnownIssue | `valid` | `GraphSQLAggregatePump.Init` still skips `HandleTableMigration` when sharded. | Refreshed `.proof/evidence/ki-graph-sql-aggregate-migrate-sharded-tables-ignored.yaml`. |
| `graylog-moesif-logfatal-on-record-error` | KnownIssue | `needs_split` -> split | Record mixed Graylog per-record fatal, Moesif per-record fatal, Moesif config-refresh fatal, and structurally unreachable marshal branches. | Withdrew this umbrella as superseded; active issues are now `graylog-record-decode-logfatal`, `moesif-record-decode-logfatal`, and `moesif-config-read-error-logfatal`. Defensive unreachable marshal/helper fatal arms are carried only by the process-exit ratchet/proof debt, not as customer-impacting KI scope. |
| `graylog-nil-client-recursive-writedata-duplicates-data` | KnownIssue | `needs_reproducer_hardening` -> hardened | Focused local-UDP tripwire now leaves `GraylogPump.client` nil, writes one record, and observes two GELF datagrams after the recursive recovery path falls through. | Updated `.proof/evidence/ki-graylog-nil-client-recursive-writedata-duplicates-data.yaml` to run `TestGraylogPump_NilClientRecursiveWriteDataDuplicatesData_KI` under the memory monitor; no external Graylog service or container is required. |
| `health-endpoint-auth-not-enforced` | KnownIssue | `needs_metadata_hardening` -> reviewed | Static source shows health route registers without auth middleware. | Refreshed `.proof/evidence/ki-health-endpoint-auth-not-enforced.yaml`; backlog: clarify whether unauthenticated liveness is accepted posture or must be auth-gated. |
| `health-endpoint-is-liveness-only` | KnownIssue | `valid` | `/health` still always returns 200 and is separate from auth/rate-limit concerns. | Refreshed `.proof/evidence/ki-health-endpoint-is-liveness-only.yaml`. |
| `health-endpoint-rate-limit-not-enforced` | KnownIssue | `needs_metadata_hardening` -> reviewed | Static source shows health route has no throttling/rate-limit wrapper. | Refreshed `.proof/evidence/ki-health-endpoint-rate-limit-not-enforced.yaml`. |
| `health-listener-bind-failure-logfatal` | KnownIssue | `needs_reproducer_hardening` -> hardened | Focused subprocess tripwire now occupies the configured wildcard port, invokes `ServeHealthCheck` in a child process, and observes the current `log.Fatal` process exit path. | Updated `.proof/evidence/ki-health-listener-bind-failure-logfatal.yaml` to run `TestServeHealthCheckOccupiedPortFatal_KI` under the memory monitor; no product code changed. |
| `http-retry-post-idempotency-unenforced` | KnownIssue | `needs_reproducer_hardening` -> partially hardened | Existing test proves POST request body replay on retry. | Added canonical `// Reproduces: http-retry-post-idempotency-unenforced`, test evidence pointer, and refreshed `.proof/evidence/ki-http-retry-post-idempotency-unenforced.yaml`; backlog: add duplicate-acceptance scenario where receiver commits but response fails. |
| `http-sdk-redirect-policy-implicit` | KnownIssue | `needs_metadata_hardening` -> reviewed | Pinned SDK source still confirms default HTTP redirect behavior. | Attached and refreshed `.proof/evidence/ki-http-sdk-redirect-policy-implicit.yaml`; still static-only SDK source evidence. |
| `hybrid-getdialfn-leaks-conn-on-handshake-fail` | KnownIssue | `needs_reproducer_hardening` -> hardened | `getDialFn` constructs the dialer internally, so a fake `net.Conn` reproducer would require a production seam. | Added `TestHybridGetDialFnWriteErrorDoesNotCloseConn_KI`, a non-invasive static control-flow tripwire that pins the write-failure early return and missing `conn.Close`; refreshed `.proof/evidence/ki-hybrid-getdialfn-leaks-conn-on-handshake-fail.yaml` under the memory monitor. |
| `hybrid-rpc-retry-duration-not-deadline-bounded` | KnownIssue | `valid` | Retry count is bounded; elapsed deadline is not specified/tested. | Refreshed `.proof/evidence/ki-hybrid-rpc-retry-duration-not-deadline-bounded.yaml`. |
| `influx-v1-unbounded-reconnect-recursion` | KnownIssue | `valid` | `connect()` still recursively calls itself after sleep on client creation error. | Attached and refreshed `.proof/evidence/ki-influx-v1-unbounded-reconnect-recursion.yaml`. |
| `instrumentation-goroutines-no-recover-or-shutdown` | KnownIssue | `needs_split` -> evidenced | Focused static tripwires now inspect instrumentation source without starting the unbounded monitor, pin missing recover guards on background goroutines, and pin the GC monitor's unbounded sleep loop without stop/cancel parameters. | Refreshed `.proof/evidence/ki-instrumentation-goroutines-no-recover-or-shutdown.yaml` and set KI `status: reviewed`; backlog remains to split panic-recovery debt from GC-monitor shutdown debt if separate remediation ownership is needed. |
| `instrumentation-statsd-channel-backpressure-blocks-emitters` | KnownIssue | `valid` | Synchronous sends to fixed `cmdChan` remain. | Attached and refreshed `.proof/evidence/ki-instrumentation-statsd-channel-backpressure-blocks-emitters.yaml`. |
| `kafka-logfatal-on-init-mech-and-timeout` | KnownIssue | `valid` | Subprocess tests exist and code still fatals on SCRAM/timeout parse failures. | No change needed. |
| `kafka-writedata-full-batch-allocation-unbounded` | KnownIssue | `valid` | Full `[]kafka.Message` allocation remains before backend write. | Attached and refreshed `.proof/evidence/ki-kafka-writedata-full-batch-allocation-unbounded.yaml`. |
| `kafka-writedata-marshal-err-structurally-unreachable` | KnownIssue | `valid` | Defensive `json.Marshal` error branch remains MC/DC/defensive-code debt. | No change needed. |
| `kafka-writedata-non-analytics-record-panic` | KnownIssue | `needs_metadata_hardening` -> reviewed | Bound reproducer exists and source still panics on unchecked assertion. | Stamped review date `2026-11-30`; tightened reproducer command and refreshed `.proof/evidence/ki-kafka-writedata-non-analytics-record-panic.yaml`. |
| `kafka-writedata-swallows-write-errors` | KnownIssue | `valid` | `TestKafkaPump_WriteData_WriteErrorPath` passed and proves writer errors are exercised without a returned error. | Tightened reproducer command and refreshed `.proof/evidence/ki-kafka-writedata-swallows-write-errors.yaml`. |
| `kinesis-batch-size-over-aws-putrecords-limit` | KnownIssue | `valid` | Focused helper test passed and helper still allows a 501-record batch. | Tightened reproducer command and refreshed `.proof/evidence/ki-kinesis-batch-size-over-aws-putrecords-limit.yaml`. |
| `kinesis-putrecords-per-record-failures-return-nil` | KnownIssue | `needs_reproducer_hardening` -> hardened | Focused static tripwire now pins the successful `PutRecords` response path where per-record `ErrorCode` values are logged and `WriteData` still returns nil. | Updated `.proof/evidence/ki-kinesis-putrecords-per-record-failures-return-nil.yaml` to run `TestKinesisPutRecordsPerRecordFailuresReturnNil_KI` under the memory monitor; no AWS service, container, or product seam is required. |
| `kinesis-random-partition-key-not-idempotent` | KnownIssue | `needs_metadata_hardening` -> reviewed | Static source evidence is accurate: random partition key is generated per record. | Attached and refreshed `.proof/evidence/ki-kinesis-random-partition-key-not-idempotent.yaml`; backlog: consider deterministic test when randomness is injectable. |
| `kinesis-splitintobatches-zero-infinite-loop` | KnownIssue | `needs_metadata_hardening` -> reviewed | Static evidence is accurate; live infinite-loop test is unsafe without a subprocess/timeout harness. | Attached and refreshed `.proof/evidence/ki-kinesis-splitintobatches-zero-infinite-loop.yaml`. |

## KnownIssue Slice: StatsD Through Mongo

Local audit completed for 25 KnownIssues from `logfatal-on-statsd-setup`
through `mongo-standard-logbrowser-compatible-index-conflict`.

| Item | Type | Verdict | Evidence Checked | Action |
| --- | --- | --- | --- | --- |
| `logfatal-on-statsd-setup` | KnownIssue | `valid` | `instrumentation_helpers.go` still calls `log.Fatal` after `NewStatsDSink` failure; in-process execution is not practical because it exits the runner, so the AST ratchet is the correct tripwire. | Added `// Reproduces: logfatal-on-statsd-setup` to `TestProcessExitOnRecoverable_NoNewLogFatalSites`, linked the test evidence, stamped review date `2026-11-30`, and refreshed `.proof/evidence/ki-logfatal-on-statsd-setup.yaml`. |
| `logzio-segment-no-shutdown-flush` | KnownIssue | `valid` | Static tripwires prove Logz.io and Segment embed `CommonPumpConfig` and do not override `Shutdown`. | Added focused reproducer command and refreshed `.proof/evidence/ki-logzio-segment-no-shutdown-flush.yaml`; reviewed through `2026-11-30`. |
| `main-prefilter-debug-logs-unfiltered-records` | KnownIssue | `needs_reproducer_hardening` -> hardened | Focused log-capture tripwire now proves a configured ignored `api_key` is removed from the forwarded record but still appears in the pre-filter debug log. | Updated `.proof/evidence/ki-main-prefilter-debug-logs-unfiltered-records.yaml` to run `TestPreprocessAnalyticsValues_DebugLogsBeforeIgnoreFields_KI` under the memory monitor; no external service or container is required. |
| `main-shutdown-pumps-serial-no-timeout` | KnownIssue | `needs_split` -> split | Record combined serial shutdown/no per-pump timeout with an `execPumpWriting` goroutine lifetime gap after context timeout. | Narrowed this KI to `checkShutdown` serial shutdown sequencing, added an in-memory blocking-shutdown tripwire, and moved the positive-timeout inner goroutine lifetime debt to `execpumpwriting-timeout-leaves-write-goroutine` under `SYS-REQ-005`/`SW-REQ-072`. |
| `main-startup-logfatal-on-transient-backend` | KnownIssue | `needs_reproducer_hardening` -> hardened | Focused static tripwire now pins `setupAnalyticsStore` and `storeVersion` transient storage constructor/Init error branches that still call `Fatal`. | Updated `.proof/evidence/ki-main-startup-logfatal-on-transient-backend.yaml` to run `TestMainStartupTransientStorageErrorsCallFatal_KI` under the memory monitor; no Redis, storage backend, subprocess, external service, or container is required. |
| `mapstructure-decode-silently-drops-unknown-keys` | KnownIssue | `valid` | Cross-pump OpenGrep evidence identifies lenient `mapstructure.Decode` calls; accepted risk already covers the config typo posture. | Attached and refreshed `.proof/evidence/ki-mapstructure-decode-silently-drops-unknown-keys.yaml`; reproducer now scans production files for lenient `mapstructure.Decode` and absence of strict `ErrorUnused`/`DecoderConfig`. |
| `mcdc-pumps-below-95` | KnownIssue | `valid` | Current full audit reports code-level MC/DC meets configured thresholds, with one accepted KI-debt row; this record remains proof-coverage context rather than product debt. | Attached and refreshed `.proof/evidence/ki-mcdc-pumps-below-95.yaml`; backlog remains to split this umbrella if we want narrower pump-family proof debt. |
| `mcp-mongo-aggregate-atomicity-fault-injection-missing` | KnownIssue | `valid` | This is explicitly a missing fault-injection harness record; static source/requirement check supports the proof-harness gap. | Attached and refreshed `.proof/evidence/ki-mcp-mongo-aggregate-atomicity-fault-injection-missing.yaml`; full fault-injection harness remains future work. |
| `mcp-sql-aggregate-background-index-concurrency-unbounded` | KnownIssue | `hardened` | SQLite witness forces the Postgres background-index branch, proves `ensureIndex(..., true)` returns nil immediately, and verifies the failed background create does not signal readiness. | Added `TestMCPSQLAggregateEnsureIndexBackgroundFailureIsNotSurfaced_KI` and refreshed `.proof/evidence/ki-mcp-sql-aggregate-background-index-concurrency-unbounded.yaml`; no product lifecycle behavior changed. |
| `mcp-sql-aggregate-mysql-create-index-syntax-broken` | KnownIssue | `hardened` | Focused static tripwire now pins that MCP SQL Aggregate only gates `CONCURRENTLY` on Postgres while `IF NOT EXISTS` remains in the DDL template used for non-Postgres paths. | Extended `TestSQLMySQLCreateIndexIfNotExists_KI` and refreshed `.proof/evidence/ki-mcp-sql-aggregate-mysql-create-index-syntax-broken.yaml`; no product DDL changed. |
| `metrics-label-cardinality-unbounded` | KnownIssue | `valid` | Manual code-signal evidence covers StatsD, DogStatsD, and Prometheus high-cardinality label/tag surfaces. | Attached and refreshed `.proof/evidence/ki-metrics-label-cardinality-unbounded.yaml`. |
| `moesif-config-response-readall-unbounded` | KnownIssue | `valid` | `pumps/moesif.go` still reads the full configuration response with `ioutil.ReadAll` before parsing. | Tightened reproducer command and refreshed `.proof/evidence/ki-moesif-config-response-readall-unbounded.yaml`. |
| `moesif-queueevent-error-swallowed` | KnownIssue | `valid` | `TestMoesifPump_WriteData_QueueEventError_KI` proves SDK enqueue failure is logged while `WriteData` returns nil. | Added test evidence pointer, stamped review date `2026-11-30`, and refreshed `.proof/evidence/ki-moesif-queueevent-error-swallowed.yaml`. |
| `moesif-raw-body-unbounded-json-mask` | KnownIssue | `needs_reproducer_hardening` -> hardened | Focused helper witness now feeds a bounded 512 KiB JSON body and depth-96 nested JSON body through `maskRawBody`, proving current masking still parses/recurses without local size/depth rejection. | Updated `.proof/evidence/ki-moesif-raw-body-unbounded-json-mask.yaml` to run `TestMoesifPump_MaskRawBodyUnboundedJSONMask_KI` under the memory monitor; no Moesif SDK client, network service, or container is required. |
| `moesif-sampling-randomness-obligation-misattributed` | KnownIssue | `needs_metadata_hardening` -> hardened | Static source shows intentional probabilistic sampling; subagent review found stale wording because `SW-REQ-052` now explicitly specifies sampling. | Rewrote KI and `SW-REQ-052` deferral to state the remaining debt is randomness-signal attribution to `determinism`, not missing sampling semantics; attached and refreshed `.proof/evidence/ki-moesif-sampling-randomness-obligation-misattributed.yaml`. |
| `mongo-aggregate-atomicity-fault-injection-missing` | KnownIssue | `valid` | This is explicitly a missing fault-injection harness record for Mongo aggregate update sequencing. | Attached and refreshed `.proof/evidence/ki-mongo-aggregate-atomicity-fault-injection-missing.yaml`; full fault-injection harness remains future work. |
| `mongo-aggregate-last-document-query-ignores-timeout` | KnownIssue | `valid` | Source still calls `m.store.Query(context.Background(), ...)` in `getLastDocumentTimestamp`. | Attached and refreshed `.proof/evidence/ki-mongo-aggregate-last-document-query-ignores-timeout.yaml`. |
| `mongo-cap-collection-non64-arch-unreachable` | KnownIssue | `valid` | 32-bit guard remains structurally unreachable on the supported 64-bit release matrix. | Attached and refreshed `.proof/evidence/ki-mongo-cap-collection-non64-arch-unreachable.yaml`. |
| `mongo-pump-ignores-caller-context` | KnownIssue | `valid` | `TestMongoPump_WriteData_IgnoresCallerCtx_KI` and code-signal evidence show canceled caller context is ignored in the standard Mongo write path; sibling Mongo-family paths remain covered by current context-drop signals. | Added memory-guarded reproducer command, corrected current source anchors, documented the container-backed reproducer, and refreshed `.proof/evidence/ki-mongo-pump-ignores-caller-context.yaml`. |
| `mongo-pump-init-connect-logfatal-unreachable` | KnownIssue | `valid` | Mongo connection/config `log.Fatal` branches remain structural process-exit gaps tied to MC/DC ignores. | Attached and refreshed `.proof/evidence/ki-mongo-pump-init-connect-logfatal-unreachable.yaml`. |
| `mongo-pump-writeuptime-nil-on-bad-msgpack` | KnownIssue | `valid` | Existing Mongo and MongoSelective bad-msgpack KI tests assert the current panic behavior. | Tightened reproducer command to cover both tests, changed release disposition to `ship_with_known_issue`, documented the container-backed reproducer, refreshed source anchors, and refreshed `.proof/evidence/ki-mongo-pump-writeuptime-nil-on-bad-msgpack.yaml`. |
| `mongo-selective-final-skipped-record-drops-pending-batch` | KnownIssue | `valid` | Existing tripwire proves a final skipped MongoSelective record can drop a pending valid batch; provenance points to `83a16f3`. | Added refreshed manifest `.proof/evidence/ki-mongo-selective-final-skipped-record-drops-pending-batch.yaml` and tightened reproducer command. |
| `mongo-standard-final-skipped-record-drops-pending-batch` | KnownIssue | `valid` | Existing tripwire proves standard Mongo can drop a pending valid batch when the final record is skipped; provenance points to `83a16f3`. | Added refreshed manifest `.proof/evidence/ki-mongo-standard-final-skipped-record-drops-pending-batch.yaml` and tightened reproducer command. |
| `mongo-standard-insert-error-double-send-goroutine-leak` | KnownIssue | `valid` | Existing AST tripwire checks the errCh double-send/early-return structure; provenance points to `14ccaba`. | Added refreshed manifest `.proof/evidence/ki-mongo-standard-insert-error-double-send-goroutine-leak.yaml` and tightened reproducer command. |
| `mongo-standard-logbrowser-compatible-index-conflict` | KnownIssue | `valid` | Existing fake-store tripwire proves compatible log-browser index conflicts are returned on non-StandardMongo paths; provenance points to `8458e11`. | Added refreshed manifest `.proof/evidence/ki-mongo-standard-logbrowser-compatible-index-conflict.yaml` and tightened reproducer command. |

## KnownIssue Slice: Panic Through Resurface

Read-only subagent audit completed for 16 KnownIssues from
`no-panic-recovery-in-exec-pump-writing` through
`resurface-writedata-blocks-on-queue-full`.

| Item | Type | Verdict | Evidence Checked | Action |
| --- | --- | --- | --- | --- |
| `no-panic-recovery-in-exec-pump-writing` | KnownIssue | `needs_merge` -> merged_alias | Duplicate of the `main.go` fanout panic surface; stronger subprocess evidence is on `pump-fanout-panic-not-recovered`, while legacy specs/problem reports referenced this ID. | Repointed current requirement deferrals to `pump-fanout-panic-not-recovered`; retained this record as a reviewed legacy alias with the same reproducer evidence for history continuity. |
| `preprocess-decode-error-leaves-nil-hole-in-keys` | KnownIssue | `needs_reproducer_hardening` -> hardened | Focused subprocess tripwire now feeds a bad msgpack payload while a filtering pump is configured and observes the current nil-slot panic in `filterData`. | Updated `.proof/evidence/ki-preprocess-decode-error-leaves-nil-hole-in-keys.yaml` to run `TestPreprocessDecodeErrorNilHolePanicsWithFilteringPump_KI` under the memory monitor; no external service or container is required. |
| `prometheus-init-mutates-default-mux` | KnownIssue | `needs_metadata_hardening` -> hardened | Focused static AST tripwire now pins `PrometheusPump.Init` registering on `http.DefaultServeMux` and serving with a nil handler, and `SW-REQ-024` now tracks the debt under `shared_state_synchronized` instead of request timeout. | Updated and refreshed `.proof/evidence/ki-prometheus-init-mutates-default-mux.yaml`; no listener is started and no product code changed. |
| `prometheus-metric-maps-race` | KnownIssue | `needs_reproducer_hardening` -> hardened | Race-build subprocess witness now constructs `PrometheusMetric` directly and concurrently calls `Inc`/`Observe`, with the parent asserting `DATA RACE` output anchored to `prometheus.go`. | Refreshed `.proof/evidence/ki-prometheus-metric-maps-race.yaml` under the memory monitor; the witness avoids `PrometheusPump.Init`, HTTP mux, registry state, external services, and containers. |
| `prometheus-metrics-auth-not-enforced` | KnownIssue | `valid` | Static source and `SW-REQ-024 auth_required` deferral match the unauthenticated metrics endpoint. | Attached and refreshed `.proof/evidence/ki-prometheus-metrics-auth-not-enforced.yaml`. |
| `protobuf-decode-nil-submessage-panic` | KnownIssue | `needs_reproducer_hardening` -> hardened | Focused serializer tripwire now asserts current panics for non-byte input and valid protobuf payloads missing `Geo`, `Network`, or `Latency`. | Refreshed `.proof/evidence/ki-protobuf-decode-nil-submessage-panic.yaml`; product decoder behavior remains unchanged. |
| `pump-fanout-no-global-concurrency-limit` | KnownIssue | `needs_reproducer_hardening` -> hardened | Focused in-memory tripwire now configures six blocking pumps and observes all six `WriteData` calls enter concurrently before release. | Updated `.proof/evidence/ki-pump-fanout-no-global-concurrency-limit.yaml` to run `TestWriteToPumpsNoGlobalConcurrencyLimit_KI` under the memory monitor; no external service or container is required. |
| `pump-fanout-panic-not-recovered` | KnownIssue | `valid` | Subprocess reproducer exists and passed; this is the canonical fanout panic KI. | Added the merged affected requirements and code-signal references from `no-panic-recovery-in-exec-pump-writing`; accepted risk `pump-panic-crashes-process` now points here conceptually. |
| `pump-no-per-pump-circuit-breaker` | KnownIssue | `valid` | Focused reproducer passed and proves a failing pump is invoked repeatedly without backoff. | Corrected root-level test paths, added focused reproducer command, and refreshed `.proof/evidence/ki-pump-no-per-pump-circuit-breaker.yaml`. |
| `pump-no-timeout-can-block-purge-cycle` | KnownIssue | `needs_reproducer_hardening` -> hardened | Focused main tripwire now invokes `execPumpWriting` with a timeout=0 pump that stays blocked until an explicit release channel is closed. | Refreshed `.proof/evidence/ki-pump-no-timeout-can-block-purge-cycle.yaml`; product timeout behavior remains unchanged. |
| `pump-writedata-swallows-per-batch-errors` | KnownIssue | `needs_reproducer_hardening` -> hardened | Table-driven static tripwire now covers the remaining true WriteData error-observability surfaces: MongoSelective, Kinesis API-level errors, SQL, Graylog no-return-channel logging, Syslog discarded writer errors, Logzio ignored sender errors, Segment ignored Track errors, Influx v1 ignored write errors, and Influx v2 undrained async errors. | Removed stale MongoAggregate scope because current `WriteData` returns `DoAggregatedWriting` errors; refreshed `.proof/evidence/ki-pump-writedata-swallows-per-batch-errors.yaml` under the memory monitor. |
| `pumps-logfatal-on-config-decode` | KnownIssue | `needs_split` | Record now mixes pure `mapstructure.Decode` fatals with non-decode setup fatals and a Mongo panic-call signal. | Attached and refreshed `.proof/evidence/ki-pumps-logfatal-on-config-decode.yaml`; updated review date to `2026-11-30`; backlog remains to split decode `log.Fatal` debt from non-decode init/setup fatal debt. |
| `resurface-disabled-writedata-closes-channel` | KnownIssue | `needs_metadata_hardening` -> hardened | Focused Resurface tripwire now closes `rp.data` through the disabled `WriteData` path, then models a stale enabled sender and proves the current send-on-closed-channel panic state. | Added and refreshed `.proof/evidence/ki-resurface-disabled-writedata-closes-channel.yaml`; product channel lifecycle behavior remains unchanged. |
| `resurface-maprawdata-empty-request-panic` | KnownIssue | `valid` | Direct panic reproducer exists and passed. | Tightened reproducer command and refreshed `.proof/evidence/ki-resurface-maprawdata-empty-request-panic.yaml`. |
| `resurface-worker-errors-swallowed` | KnownIssue | `needs_reproducer_hardening` -> hardened | Focused Resurface tripwire now enqueues malformed worker inputs, observes nil `WriteData` and `Flush` returns, and confirms no Resurface message is queued. | Refreshed `.proof/evidence/ki-resurface-worker-errors-swallowed.yaml`; product worker error propagation remains unchanged. |
| `resurface-writedata-blocks-on-queue-full` | KnownIssue | `needs_reproducer_hardening` -> hardened | Focused Resurface tripwire now fills the enabled queue, proves `WriteData` remains blocked while context is alive, then cancels to release. | Refreshed `.proof/evidence/ki-resurface-writedata-blocks-on-queue-full.yaml`; product queue behavior remains unchanged. |

## KnownIssue Slice: Retry Through SQL Batch

Read-only subagent audit completed for 20 KnownIssues from
`retry-4xx-bodyread-fail-causes-retry` through
`sql-batch-size-zero-infinite-loop`.

| Item | Type | Verdict | Evidence Checked | Action |
| --- | --- | --- | --- | --- |
| `retry-4xx-bodyread-fail-causes-retry` | KnownIssue | `needs_reproducer_hardening` -> hardened | Focused retry tripwire now returns a 400 response with a failing body reader and asserts current retry count reaches two attempts. | Refreshed `.proof/evidence/ki-retry-4xx-bodyread-fail-causes-retry.yaml`; `SW-REQ-030` and `INT-REQ-006` remain affected because the retry classifier surfaces a non-permanent error for a non-retryable response. |
| `retry-backoff-duration-not-deadline-bounded` | KnownIssue | `needs_metadata_hardening` -> hardened | Record now separates the bounded-attempt implementation from the missing elapsed-time product policy, so the accepted debt is spec/proof coverage rather than a claimed runtime defect. | Clarified mitigation/remediation and kept `.proof/evidence/ki-retry-backoff-duration-not-deadline-bounded.yaml` as the static proof-debt witness. |
| `retry-buffers-full-request-body-in-memory` | KnownIssue | `valid` | Code and obligation mapping match `io.ReadAll(req.Body)` in retry middleware. | Tightened reproducer command and refreshed `.proof/evidence/ki-retry-buffers-full-request-body-in-memory.yaml`. |
| `retry-row5-mcdc-backoff-not-injectable` | KnownIssue | `needs_metadata_hardening` -> hardened | Record now explicitly classifies the slow row-5 witness as proof-harness testability debt, not evidence that retry classification is wrong. | Added monitored reproducer command and clarified mitigation/remediation; no product code changed. |
| `serializer-protobuf-loses-city-names` | KnownIssue | `needs_reproducer_hardening` -> hardened | Focused protobuf round-trip tripwire now encodes `Geo.City.Names`, decodes the record, and asserts the current nil names map. | Refreshed `.proof/evidence/ki-serializer-protobuf-loses-city-names.yaml`; product serializer behavior remains unchanged. |
| `serializer-protobuf-loses-graphql-error-path` | KnownIssue | `valid` | Focused KI test exists and passed; affected requirements and obligations align. | Added focused reproducer command and refreshed `.proof/evidence/ki-serializer-protobuf-loses-graphql-error-path.yaml`. |
| `setaggregatetimestamp-mcdc-unreachable-not-ok` | KnownIssue | `needs_metadata_hardening` -> evidenced | Structural MC/DC debt is real and code now has the `//mcdc:ignore`; the record remains a proof-debt tracker for the unreachable independent-effect row. | Attached and refreshed `.proof/evidence/ki-setaggregatetimestamp-mcdc-unreachable-not-ok.yaml`; backlog remains only to tighten remediation wording. |
| `splunk-filtertags-skips-consecutive-matches` | KnownIssue | `needs_reproducer_hardening` -> hardened | Focused Splunk tripwire now filters consecutive ignored-prefix tags and asserts the current retained middle tag. | Refreshed `.proof/evidence/ki-splunk-filtertags-skips-consecutive-matches.yaml`; product filtering behavior remains unchanged. |
| `splunk-newsplunkclient-mutates-default-transport` | KnownIssue | `needs_reproducer_hardening` -> hardened | Focused Splunk tripwire now snapshots/restores `http.DefaultClient.Transport`, calls `newSplunkClient`, and proves the current global transport replacement plus default-client ownership. | Refreshed `.proof/evidence/ki-splunk-newsplunkclient-mutates-default-transport.yaml`; product client construction remains unchanged. |
| `splunk-writedata-non-analytics-record-panic` | KnownIssue | `needs_reproducer_hardening` -> hardened | Existing tripwire now uses `require.Panics`, so it fails if the unchecked assertion is guarded while the KI remains open. | Refreshed `.proof/evidence/ki-splunk-writedata-non-analytics-record-panic.yaml`; no product code changed. |
| `sql-aggregate-atomicity-fault-injection-missing` | KnownIssue | `needs_reproducer_hardening` -> hardened | Focused SQLite tripwire now injects a GORM Create callback error into `DoAggregatedWriting`, observes the current returned error, and verifies no aggregate rows are committed. | Refreshed `.proof/evidence/ki-sql-aggregate-atomicity-fault-injection-missing.yaml`; product upsert behavior remains unchanged. |
| `sql-aggregate-background-index-concurrency-unbounded` | KnownIssue | `needs_reproducer_hardening` -> hardened | Focused SQLite tripwire now forces the Postgres `CONCURRENTLY` background index branch against SQLite, proves `ensureIndex(..., true)` returns nil immediately, and verifies readiness is not signaled after the background failure. | Refreshed `.proof/evidence/ki-sql-aggregate-background-index-concurrency-unbounded.yaml`; product background lifecycle behavior remains unchanged. |
| `sql-aggregate-mysql-create-index-if-not-exists-unsupported` | KnownIssue | `needs_reproducer_hardening` -> hardened | Focused static tripwire now pins that only `CONCURRENTLY` is Postgres-gated while `IF NOT EXISTS` remains in the SQL Aggregate non-Postgres index DDL template. | Refreshed `.proof/evidence/ki-sql-aggregate-mysql-create-index-if-not-exists-unsupported.yaml`; no product DDL changed. |
| `sql-aggregate-mysql-excluded-keyword-broken` | KnownIssue | `needs_reproducer_hardening` -> hardened | Focused static tripwire now pins the shared `OnConflictAssignments` temp-table qualification and all three aggregate callers still passing the Postgres-specific `"excluded"` qualifier. | Refreshed `.proof/evidence/ki-sql-aggregate-mysql-excluded-keyword-broken.yaml`; no product upsert behavior changed. |
| `sql-aggregate-no-deadlock-retry` | KnownIssue | `hardened` | SQLite/GORM callback witness injects a deadlock-looking create error and proves `DoAggregatedWriting` returns it after exactly one create attempt. | Added `TestSQLAggregateDoAggregatedWritingDoesNotRetryDeadlock_KI` and refreshed `.proof/evidence/ki-sql-aggregate-no-deadlock-retry.yaml`; no product retry logic added. |
| `sql-aggregate-sharded-shared-db-race` | KnownIssue | `needs_reproducer_hardening` -> hardened | Focused race-build subprocess witness now runs concurrent sharded `SQLAggregatePump.WriteData` calls against one pump instance and requires race-detector output from `sql_aggregate.go`. | Refreshed `.proof/evidence/ki-sql-aggregate-sharded-shared-db-race.yaml`; witness uses SQLite temp-file tables and no containers, and product shared DB mutation remains unchanged. |
| `sql-aggregate-sharded-upsert-targets-base-table` | KnownIssue | `valid` | High-severity reviewed KI still matches current code and upstream fix note. | Attached and refreshed `.proof/evidence/ki-sql-aggregate-sharded-upsert-targets-base-table.yaml`; no product fix here, keep as KnownIssue. |
| `sql-aggregate-upsert-order-undocumented` | KnownIssue | `hardened` | SW-REQ-067 now explicitly documents that SQL aggregate row order is unspecified and correctness is id-keyed by deterministic aggregate id plus `ON CONFLICT`; remaining debt is proof-model/obligation attribution, not product behavior. | Updated SW-REQ-067 YAML/docs, narrowed KI risk to compliance/proof debt, and refreshed `.proof/evidence/ki-sql-aggregate-upsert-order-undocumented.yaml`. |
| `sql-background-index-concurrency-unbounded` | KnownIssue | `hardened` | Subprocess SQLite witness forces the Postgres background-index branch, verifies the background lifecycle/error-surfacing gap, and accepts the historical SQLPump background-index panic if it occurs. | Added `TestSQLPumpEnsureIndexBackgroundFailureIsNotSurfaced_KI` and refreshed `.proof/evidence/ki-sql-background-index-concurrency-unbounded.yaml`; no product lifecycle behavior changed. |
| `sql-batch-size-zero-infinite-loop` | KnownIssue | `needs_split` -> partially hardened | Focused static tripwire now scans the six SQL-family source files and proves the current source shape defaults only `BatchSize == 0` while batch loops still advance by `BatchSize` without `<= 0` or `< 1` validation. | Refreshed `.proof/evidence/ki-sql-batch-size-zero-infinite-loop.yaml`; backlog remains to split/retire the stale zero-only title and keep a precise negative/non-positive validation KI. |

## KnownIssue Slice: SQL Defaults Through Stdout

Local audit completed for 8 KnownIssues from `sql-default-migration-today-only`
through `stdout-writedata-swallows-ctx-error`.

| Item | Type | Verdict | Evidence Checked | Action |
| --- | --- | --- | --- | --- |
| `sql-default-migration-today-only` | KnownIssue | `valid` | `TestMCDC_KI_DefaultMigrationTodayOnly` directly proves default `MigrateShardedTables=false` leaves prior-day shard schema untouched. | Stamped review date `2026-11-30`; attached and refreshed `.proof/evidence/ki-sql-default-migration-today-only.yaml`. |
| `sql-ensureindex-name-mismatch-prevents-skip` | KnownIssue | `valid` | Static code path remains: `HasIndex` name lacks the underscore used by `createIndex`; duplicate create is guarded by DDL. | Attached and refreshed `.proof/evidence/ki-sql-ensureindex-name-mismatch-prevents-skip.yaml`; a stronger backend/fake-migrator test is still useful but not required for KI quality floor. |
| `sql-schema-no-version-policy` | KnownIssue | `valid` | Static source check correctly shows AutoMigrate-only schema management and no schema-version manifest/table policy. | Attached and refreshed `.proof/evidence/ki-sql-schema-no-version-policy.yaml`. |
| `sql-standard-mysql-create-index-if-not-exists-unsupported` | KnownIssue | `needs_reproducer_hardening` -> hardened | Focused static tripwire now pins that only `CONCURRENTLY` is Postgres-gated while `IF NOT EXISTS` remains in the standard SQL non-Postgres index DDL template. | Refreshed `.proof/evidence/ki-sql-standard-mysql-create-index-if-not-exists-unsupported.yaml`; no product DDL changed. |
| `sqs-batch-partial-failures-ignored` | KnownIssue | `valid` | Added focused mock SQS test proving `SendMessageBatchOutput.Failed` is ignored when API-level error is nil. | Added evidence pointer, stamped review date `2026-11-30`, and refreshed `.proof/evidence/ki-sqs-batch-partial-failures-ignored.yaml`. |
| `sqs-batchlimit-zero-infinite-loop` | KnownIssue | `valid` | Executing the zero-limit path would hang; added bounded static tripwire for the loop shape. | Added evidence pointer, security-detail metadata, and refreshed `.proof/evidence/ki-sqs-batchlimit-zero-infinite-loop.yaml`; already reviewed through `2026-11-30`. |
| `sqs-malformed-record-sends-empty-entry` | KnownIssue | `valid` | Existing KI test proves a malformed input leaves a zero-value SQS entry in the sent batch. | Stamped review date `2026-11-30`; refreshed `.proof/evidence/ki-sqs-malformed-record-sends-empty-entry.yaml`. |
| `stdout-writedata-swallows-ctx-error` | KnownIssue | `valid` | Added focused KI test proving canceled context returns nil instead of `ctx.Err()`. | Added evidence pointer, stamped review date `2026-11-30`, and refreshed `.proof/evidence/ki-stdout-writedata-swallows-ctx-error.yaml`. |

## KnownIssue Slice: Storage Through Write Failure

Subagent audit completed for 20 KnownIssues from
`storage-connector-singleton-race` through
`write-failure-after-pop-loses-records`.

| Item | Type | Verdict | Evidence Checked | Action |
| --- | --- | --- | --- | --- |
| `storage-connector-singleton-race` | KnownIssue | `hardened` | Race-build subprocess witness concurrently initializes force-reconnect temporal storage handlers and verifies the child race detector reports DATA RACE from temporal_storage.go package-global connector/logPrefix access. | Added `TestTemporalStorageHandler_Init_ConnectorSingletonRace_KI` and refreshed `.proof/evidence/ki-storage-connector-singleton-race.yaml`; no Redis container or product synchronization change. |
| `storage-createconnector-kv-list-err-unreachable` | KnownIssue | `valid` | The listed KV/list constructor error branches remain structural/unreachable proof debt because `createConnector` constructs RedisV9 before the helper calls. | Attached and refreshed `.proof/evidence/ki-storage-createconnector-kv-list-err-unreachable.yaml`. |
| `storage-ensureconnection-error-path-unreachable` | KnownIssue | `valid` | The ensure-connection final-error path remains structural/unreachable proof debt under unbounded retry with no `backoff.Permanent` return. | Attached and refreshed `.proof/evidence/ki-storage-ensureconnection-error-path-unreachable.yaml`. |
| `storage-retry-maxelapsed-zero-is-unbounded` | KnownIssue | `valid` | `TestGetTemporalStorageExponentialBackoff` passed and still covers the unbounded retry elapsed-time posture. | Tightened reproducer command and refreshed `.proof/evidence/ki-storage-retry-maxelapsed-zero-is-unbounded.yaml`. |
| `sw-req-009-demo-randomness-obligation-misattributed` | KnownIssue | `valid` | Static source and requirement inspection show demo randomness is proof attribution debt, not the analytics requirement owner. | Attached and refreshed `.proof/evidence/ki-sw-req-009-demo-randomness-obligation-misattributed.yaml`. |
| `sw-req-060-hash-format-obligation-misattributed` | KnownIssue | `valid` | Hash-format obligation ownership remains misattributed to the broad aggregate requirement rather than the true hash contract. | Attached and refreshed `.proof/evidence/ki-sw-req-060-hash-format-obligation-misattributed.yaml`. |
| `sw-req-073-parameterized-read-obligation-misattributed` | KnownIssue | `valid` | Parameterized-read ownership remains a proof/spec attribution issue across uptime reads rather than direct `SW-REQ-073` behavior. | Attached and refreshed `.proof/evidence/ki-sw-req-073-parameterized-read-obligation-misattributed.yaml`. |
| `syslog-init-logfatal-on-invalid-transport` | KnownIssue | `needs_metadata_hardening` -> hardened | `TestSyslogPump_initConfigs_InvalidTransport_InProcess` passed and proves the fatal path is represented; record now names the accepted malformed-config process-exit risk. | Set monitored reproducer command and added release-disposition environment note; no product code changed. |
| `syslog-initwriter-logfatal-on-dial-error` | KnownIssue | `needs_metadata_hardening` -> hardened | `TestSyslogPump_initWriter_BadDial_(Subprocess|InProcess)` passed and covers dial-error fatal behavior; record now names the accepted unreachable-destination process-exit risk. | Set monitored reproducer command and added release-disposition environment note; no product code changed. |
| `systemconfig-omitdetailedrecording-unused` | KnownIssue | `needs_metadata_hardening` -> hardened | Focused tripwire now sets global `SystemConfig.OmitDetailedRecording=true`, leaves the per-pump flag false, and proves raw payloads still survive `filterData`; record now frames this as a global-config-surface mismatch against a per-backend SYS-REQ-015 contract. | Added and refreshed `.proof/evidence/ki-systemconfig-omitdetailedrecording-unused.yaml`; set `release_disposition: ship_with_known_issue`; no product code changed. |
| `temporal-storage-operations-ignore-caller-cancellation` | KnownIssue | `valid` | Static evidence still shows storage/retry paths use background contexts rather than propagating caller cancellation. | Attached and refreshed `.proof/evidence/ki-temporal-storage-operations-ignore-caller-cancellation.yaml`. |
| `temporal-storage-wire-format-unversioned` | KnownIssue | `needs_reproducer_hardening` -> partially hardened | Focused storage tripwire now proves `GetAndDeleteSet` still returns opaque payload strings unchanged and has no storage-level decode/unmarshal/version handling. | Updated `.proof/evidence/ki-temporal-storage-wire-format-unversioned.yaml` to run `TestTemporalStorageWireFormatUnversioned_KI` under the memory monitor; backlog remains to add positive versioned golden fixture compatibility evidence before closing the KI. |
| `tls-insecure-skip-verify-allowed` | KnownIssue | `valid` | Static evidence still shows configuration surfaces that allow `InsecureSkipVerify`; this is an accepted operator-risk posture. | Attached and refreshed `.proof/evidence/ki-tls-insecure-skip-verify-allowed.yaml`. |
| `uptime-aggregate-erasstr-itoa-always-nonempty` | KnownIssue | `needs_metadata_hardening` -> hardened | `TestAggregateUptimeData_TCPErrorWithURL` passed and the record now states this is structural MC/DC debt: `strconv.Itoa` always returns a non-empty string, so the false side is unreachable and covered by the current `mcdc:ignore`. | Set monitored reproducer, `status: reviewed`, and `release_disposition: ship_with_known_issue`; no product code changed. |
| `uptime-aggregate-nil-errormap-on-tcp-then-http` | KnownIssue | `needs_reproducer_hardening` -> hardened | Added focused panic tripwire `TestAggregateUptimeData_TCPErrorBeforeHTTP_KI` proving the TCP-error-first then HTTP record ordering still panics on nil `ErrorMap`. | Attached and refreshed `.proof/evidence/ki-uptime-aggregate-nil-errormap-on-tcp-then-http.yaml`; no product fix made. |
| `uptime-onconflict-request-time-is-zero-unreachable` | KnownIssue | `needs_metadata_hardening` -> hardened | `TestOnConflictUptimeAssignments_RequestTimeZeroSkipped` passed and the record now states this is structural MC/DC debt: `OnConflictUptimeAssignments` constructs an empty counter, so the non-zero `RequestTime` branch is unreachable and covered by the current `mcdc:ignore`. | Set monitored reproducer, `status: reviewed`, and `release_disposition: ship_with_known_issue`; no product code changed. |
| `uptime-pump-init-error-ignored` | KnownIssue | `hardened` | AST witness proves both uptime init branches still call `UptimePump.Init(...)` as unchecked expression statements and then log uptime initialization. | Added `TestInitialiseUptimePumpIgnoresInitError_KI` and refreshed `.proof/evidence/ki-uptime-pump-init-error-ignored.yaml`; runtime backend fault witness remains impractical without changing product seams or using external services. |
| `uptime-pump-not-shutdown` | KnownIssue | `hardened` | Lifecycle witness proves `checkShutdown` shuts down analytics pumps but never calls a shutdown-capable object stored in the separate `UptimePump` global. | Added `TestCheckShutdownSkipsUptimePump_KI` and refreshed `.proof/evidence/ki-uptime-pump-not-shutdown.yaml`. |
| `uptime-write-has-no-error-return` | KnownIssue | `valid` | Static interface/source evidence still shows uptime writes have no caller-visible error return. | Attached and refreshed `.proof/evidence/ki-uptime-write-has-no-error-return.yaml`. |
| `write-failure-after-pop-loses-records` | KnownIssue | `hardened` | In-memory witness pops an analytics record from fake storage, dispatches it to an all-failing pump set, and proves no record remains retained, requeued, or dead-lettered. | Added `TestWriteFailureAfterPopLosesRecords_KI` and refreshed `.proof/evidence/ki-write-failure-after-pop-loses-records.yaml`; no product fix made. |

## Verification Commands

- `go test -p=1 -count=1 -timeout=20m ./pumps -run '^(TestElasticsearchPump_ApiKeyAuthDroppedWhenUseSSL_KI|TestGetMapping_DecodeBase64MalformedInput_KI|TestElasticsearchPump_WriteData_MCPIndexRouting_NonBulkBug|TestElasticsearchPump_WriteData_V7ProcessDataIndexError)$'`
- `PATH=/Users/buger/go/bin:$PATH PROOF_MONITOR_MIN_FREE_PERCENT=3 PROOF_MONITOR_KILL_FREE_PERCENT=2 PROOF_MONITOR_KILL_SAMPLES=5 ./bin/run-monitored go test -p=1 -count=1 -timeout=180s -run '^(TestSQSPump_WriteData_PartialBatchFailureIgnored_KI|TestSQSPump_WriteData_MalformedRecordLeavesEmptyEntry_KI|TestSQSPumpBatchLimitZeroInfiniteLoop_KI|TestStdOutPump_WriteData_ContextCancelled_KI)$' ./pumps`
- `PATH=/Users/buger/go/bin:$PATH PROOF_MONITOR_MIN_FREE_PERCENT=3 PROOF_MONITOR_KILL_FREE_PERCENT=2 PROOF_MONITOR_KILL_SAMPLES=5 ./bin/run-monitored go test -p=1 -count=1 -timeout=180s -run '^TestMCDC_KI_DefaultMigrationTodayOnly$' ./pumps`
- `PATH=/Users/buger/go/bin:$PATH PROOF_MONITOR_MIN_FREE_PERCENT=3 PROOF_MONITOR_KILL_FREE_PERCENT=2 PROOF_MONITOR_KILL_SAMPLES=5 ./bin/run-monitored /Users/buger/go/bin/proof audit --check annotation_validity --check known_issue_complete --check known_issues_reviewed --max-findings 0`
- `PATH=/Users/buger/go/bin:$PATH PROOF_MONITOR_MIN_FREE_PERCENT=3 PROOF_MONITOR_KILL_FREE_PERCENT=2 PROOF_MONITOR_KILL_SAMPLES=5 ./bin/run-monitored /Users/buger/go/bin/proof known-issue check`
- `/Users/buger/go/bin/proof evidence validate --strict $(find .proof/evidence -name 'ki-*.yaml' | sort)` -> `valid: 29 evidence profile result(s)`
- `/Users/buger/go/bin/proof evidence validate --strict $(find .proof/evidence -name 'ki-*.yaml' | sort)` -> `valid: 30 evidence profile result(s)` after adding timeout-zero evidence.
- `/Users/buger/go/bin/proof evidence validate --strict $(find .proof/evidence -name 'ki-*.yaml' | sort)` -> `valid: 41 evidence profile result(s)` after adding the static health/hybrid/graph/external/elapsed evidence batch.
- `/Users/buger/go/bin/proof evidence validate --strict $(find .proof/evidence -name 'ki-*.yaml' | sort)` -> `valid: 52 evidence profile result(s)` after adding the Kinesis/Kafka/Resurface/Retry/Moesif/Splunk/Uptime evidence batch.
- `/Users/buger/go/bin/proof evidence validate --strict $(find .proof/evidence -name 'ki-*.yaml' | sort)` -> `valid: 65 evidence profile result(s)` after adding the backend/static KI evidence batch and refreshing the Moesif attribution record.
- `/Users/buger/go/bin/proof evidence validate --strict $(find .proof/evidence -name 'ki-*.yaml' | sort)` -> `valid: 81 evidence profile result(s)` after adding the aggregate/config/CSV/protobuf/Splunk/storage/SQL proof evidence batch.
- `/Users/buger/go/bin/proof evidence validate --strict $(find .proof/evidence -name 'ki-*.yaml' | sort)` -> `valid: 92 evidence profile result(s)` after adding the analytics/main/prometheus/instrumentation/Kinesis/Graylog evidence batch.
- `/Users/buger/go/bin/proof evidence validate --strict $(find .proof/evidence -name 'ki-*.yaml' | sort)` -> `valid: 107 evidence profile result(s)` after adding the retry/SDK/Resurface/SQL/storage/uptime static-tripwire evidence batch.
- `/Users/buger/go/bin/proof evidence validate --strict $(find .proof/evidence -name 'ki-*.yaml' | sort)` -> `valid: 117 evidence profile result(s)` after adding the aggregate/preprocess/config/serializer static-tripwire evidence batch.
- `PATH=/Users/buger/go/bin:$PATH PROOF_MONITOR_MIN_FREE_PERCENT=3 PROOF_MONITOR_KILL_FREE_PERCENT=2 PROOF_MONITOR_KILL_SAMPLES=5 ./bin/run-monitored /Users/buger/go/bin/proof evidence validate --strict $(find .proof/evidence -name 'ki-*.yaml' | sort)` -> `valid: 127 evidence profile result(s)` after attaching the ten documentation KnownIssue reproducers.
- `PATH=/Users/buger/go/bin:$PATH PROOF_MONITOR_MIN_FREE_PERCENT=3 PROOF_MONITOR_KILL_FREE_PERCENT=2 PROOF_MONITOR_KILL_SAMPLES=5 ./bin/run-monitored /Users/buger/go/bin/proof evidence validate --strict $(find .proof/evidence -name 'ki-*.yaml' | sort)` -> `valid: 137 evidence profile result(s)` after adding the remaining structural/runtime KnownIssue evidence and refreshing dependent uptime manifests.

Latest focused Proof result: `Errors: 0`, `Warnings: 0` for
`annotation_validity`, `known_issue_complete`, and `known_issues_reviewed`.
Latest `proof known-issue check` result after the evidence/security-detail
slice: exit code `0`; summary
`status=open:49,reviewed:94 severity=high:7,low:54,medium:82 cve_surface=possible:7 security_relevant=7`;
filesystem inventory also has `withdrawn:1`.
Remaining closure debt is `0` missing current reproducer-evidence manifests;
security-detail metadata findings are closed. Residual quality backlog is now
merge/split/refinement work rather than proof quality-floor failure.

Latest targeted full-audit follow-up:
`proof audit --check orphan_tests_clean --check problem_reports_reviewed --check known_issue_complete --check annotation_validity --max-findings 0`
reported `Errors: 0`, `Warnings: 0`; `proof audit --check tests_pass --check code_mcdc_measure --check code_mcdc_coverage --max-findings 0`
reported `Errors: 0`, `Warnings: 0` with all configured MC/DC tests passing,
code MC/DC within thresholds, and `mcdc_coverage` at 431 witness rows,
0 uncovered rows, and 1 accepted KI-debt row.

Tracked-debt follow-up: the only live stakeholder `witness_deferred`
(`STK-REQ-002:AC-003`) now carries `owner: tyk-team`,
`review_date: 2026-11-30`, and
`release_disposition: ship_with_known_issue`. The stale deferred-witness
backlog Section B was reduced from 10 historical rows to the one current row.
