id: mcdc-analytics-below-95
title: Analytics package code MC/DC below 95% threshold
description: Analytics package code MC/DC is 69% decisions / 75% conditions against the strict 95/95 bar configured in proof.yaml. Major gaps are in analytics/aggregate.go:incrementAggregate (deep field-switch independence pairs across APIID/ResponseCode/APIVersion/APIKey/OauthID/Geo/Tags/TrackPath branches) and analytics/uptime_data.go:AggregateUptimeData (URL/-1/ServerError/TCPError combinations). GeoIP-DB-bound branches are already excluded via //mcdc:ignore.
affected_requirements:
    - SW-REQ-011
    - SW-REQ-009
severity: low
mitigation: Per-package threshold tracking via this KI; the spec records the gap honestly rather than silently lowering the threshold.
remediation: 'Write additional analytics tests targeting (a) incrementAggregate field-switch independence pairs (one test per atomic condition while holding others constant), (b) uptime_data.go AggregateUptimeData branches with explicit boolean coverage, (c) targeted //mcdc:ignore on any remaining defensive branches not yet annotated. Estimated effort: ~1 working session of focused MC/DC test authoring.'
owner: tyk-team
release_disposition: ship
status: open
history:
    - at: "2026-05-30T09:29:29Z"
      by: human:buger
      action: created
